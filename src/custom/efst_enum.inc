// [SATHENA-SEAM stub] efst_enum.inc — intentionally empty. A consumer build shadows this file
// (its custom/ dir is first on the include path) to reserve explicit-value ids. See the
// enum-include seam comments at the include sites.
